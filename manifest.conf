# format: repo/relative/path|~/absolute/target/path
# install.sh symlinks each entry into place. Add a new line here to sync
# another tool's config; no other code changes needed.
herdr/config.toml|~/.config/herdr/config.toml
pi/settings.json|~/.pi/agent/settings.json
pi/models-store.json|~/.pi/agent/models-store.json
